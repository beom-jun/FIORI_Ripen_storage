_schema-version: "3.2"
ID: ripeninigripeninig
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: ripeninigripeninig-destination-content
  type: com.sap.application.content
  requires:
  - name: ripeninigripeninig-destination-service
    parameters:
      content-target: true
  - name: ripeninigripeninig-repo-host
    parameters:
      service-key:
        name: ripeninigripeninig-repo-host-key
  - name: ripeninigripeninig-uaa
    parameters:
      service-key:
        name: ripeninigripeninig-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: ripeninigripeninig_html_repo_host
          ServiceInstanceName: ripeninigripeninig-html5-service
          ServiceKeyName: ripeninigripeninig-repo-host-key
          sap.cloud.service: ripeninigripeninig
        - Authentication: OAuth2UserTokenExchange
          Name: ripeninigripeninig_uaa
          ServiceInstanceName: ripeninigripeninig-xsuaa-service
          ServiceKeyName: ripeninigripeninig-uaa-key
          sap.cloud.service: ripeninigripeninig
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: ripeninigripeninig-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ripeninigripeninig-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ripeninigripeninig.zip
      name: ripeninigripeninig
      target-path: resources/
- name: ripeninigripeninig
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ripeninigripeninig-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: ripeninigripeninig-destination-service
    service-plan: lite
- name: ripeninigripeninig-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: ripeninigripeninig-xsuaa-service
    service-plan: application
- name: ripeninigripeninig-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ripeninigripeninig-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
